package spygame;

public class Leaderboard {

    String playerid;
    int playerscore;

    public Leaderboard(String name, int score){
            playerid = name;
            playerscore = score;
    }

    if(event.getSource()==newmatch)

    {
        if (p1score != 0 || p2score != 0)
        {
            Leaderboard x; 

            if (p1score > p2score)
            {
                x = new Leaderboard (player1name, p1score);
            }
            
}
